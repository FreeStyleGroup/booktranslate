/* `/llms.txt` — описание сайта для языковых моделей.
 *
 * Соглашение llmstxt.org: заголовок, краткая суть цитатой, разделы со
 * ссылками. Модели, которые собирают сведения о продукте, читают этот файл
 * вместо разметки главной — и получают то же, что человек, только без
 * вёрстки. Написано вручную, а не собрано из страницы: страница сделана
 * для чтения глазами, а здесь важны факты, а не подача.
 *
 * Статический ответ: содержимое меняется только вместе с выкатом. */

import { CONTACT_EMAIL, SITE_DESCRIPTION, SITE_NAME, SITE_URL } from "../contacts";

export const dynamic = "force-static";

const TEXT = `# ${SITE_NAME}

> ${SITE_DESCRIPTION}

${SITE_NAME} — сервис перевода технических книг и документации с английского
на русский. Не «текст на другом языке», а готовая к работе книга: в своём
формате, со своей терминологией и с понятным списком мест, которые стоит
перечитать. Для студентов и исследователей (учебник, монография, статья,
диплом) и для компаний и бюро переводов (тома документации, серии
руководств, локализация продукта).

## Как работает

1. Загрузка книги: DOCX, EPUB, HTML, Markdown или текст. Документ
   раскладывается на сегменты — заголовки, абзацы, пункты списков, ячейки
   таблиц — и структура запоминается.
2. Терминология до перевода: система собирает то, что в книге повторяется,
   человек решает один раз. Пока остаются нерешённые слова, перевод не
   начинается — обойти можно, но это осознанный шаг.
3. Справка по незнакомому слову: определение, принятый в отрасли перевод,
   расшифровка сокращения и адреса источников; найденное остаётся в
   каталоге организации.
4. Перевод с ограничениями: модель получает сегмент, соседние абзацы,
   заголовок раздела и термины как требование. Повторы и совпадения с
   памятью переводов не переводятся заново.
5. Шесть проверок на каждом сегменте: числа и единицы измерения,
   подстановки и разметка, употребление терминов, раскрытие сокращений при
   первом употреблении, непереведённые куски, пустой перевод. Спорное
   собирается в очередь замечаний; правка одного сегмента расходится по его
   повторам; принимает работу человек.
6. Выгрузка: файл того же формата. DOCX, EPUB и HTML переписываются по
   месту — оформление, картинки, таблицы, оглавление и стили остаются.
   Недопереведённая книга не отдаётся молча: черновик помечен числом
   блоков, оставшихся исходным текстом.

## Форматы

- Приём: PDF с текстовым слоем, DOCX, EPUB, HTML, Markdown, TXT.
- Выгрузка в формате оригинала: DOCX, EPUB, HTML.
- Выгрузка новым документом Word, текстом или в Markdown: для любого исходника.
- Сканы PDF пока не распознаются; XLIFF принимается, но пока не разбирается.

## Учёт и данные

- Каждый запуск отчитывается: сколько сегментов закрыто памятью и
  повторами, сколько ушло модели и во что это обошлось. Хранятся токены,
  а не деньги: цены меняются, а потраченное на книгу — исторический факт.
- Данные организаций изолированы на уровне модели данных. Внешний поиск
  справок по умолчанию выключен. Провайдер перевода выбирается настройкой,
  включая собственное развёртывание модели.
- Перевод идёт на сервере: вкладку можно закрыть, по готовности приходит
  уведомление на почту.

## Страницы

- [Главная](${SITE_URL}/): что это, как работает, форматы, вопросы и ответы.
- [Регистрация](${SITE_URL}/register): заявка на рабочее пространство.
- [Вход](${SITE_URL}/login): кабинет для тех, у кого пространство уже есть.

## Контакты

- Почта: ${CONTACT_EMAIL}

## In English

${SITE_NAME} translates technical books and documentation from English into
Russian. Terminology is settled before translation and enforced across the
whole book; every segment passes six checks (numbers and units, placeholders
and markup, glossary usage, abbreviation expansion, untranslated fragments,
empty output); DOCX, EPUB and HTML are rewritten in place, so formatting,
images, tables and the table of contents survive. Translation memory and
in-book repetitions are reused instead of being re-translated. Interface is
in Russian; English is in progress.
`;

export function GET(): Response {
  return new Response(TEXT, {
    headers: { "content-type": "text/plain; charset=utf-8" },
  });
}
